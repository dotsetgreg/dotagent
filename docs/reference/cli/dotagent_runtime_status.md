# dotagent runtime status

## dotagent runtime status

Show runtime + readiness status

```text
dotagent runtime status [flags]
```

### Options

```text
      --check           Exit non-zero when runtime is not ready
      --format string   Output format: text|json (default "text")
  -h, --help            help for status
```

### Options inherited from parent commands

```text
      --instance string   Instance ID under ~/.dotagent/instances (default "default")
```

### SEE ALSO

* [dotagent runtime](dotagent_runtime.md)   - Manage Docker runtime lifecycle for an instance
