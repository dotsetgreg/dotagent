# Reference Docs

This folder contains generated references and should be updated by running:

```bash
make docs-gen
```

Generated artifacts:
- CLI markdown (`cli/`)
- Man pages (`man/`)
- config/provider/tool references (`*.md`)
